import { useRef, useState } from 'react';
import { FileAudio, FileVideo, Image, LoaderCircle, Trash2, UploadCloud } from 'lucide-react';
const valid = ['image/jpeg','image/png','image/webp','video/mp4','video/quicktime','audio/mpeg','audio/wav'];
const iconFor = type => type.startsWith('image') ? <Image/> : type.startsWith('video') ? <FileVideo/> : <FileAudio/>;
export default function UploadBox({ onAnalyze }) { const ref=useRef(); const [file,setFile]=useState(null), [error,setError]=useState(''); const [drag,setDrag]=useState(false); const [loading,setLoading]=useState(false); const accept=(f)=>{if(!f)return;if(!valid.includes(f.type)){setError('Please choose a JPG, PNG, WEBP, MP4, MOV, MP3, or WAV file.');return}if(f.size>500*1024*1024){setError('Files must be 500 MB or smaller.');return}setError('');setFile(f)}; const submit=async()=>{if(!file)return;setLoading(true);setError('');try{await onAnalyze(file)}catch(e){setError(e.message)}finally{setLoading(false)}};
 return <div className="upload-box"><input ref={ref} type="file" hidden accept=".jpg,.jpeg,.png,.webp,.mp4,.mov,.mp3,.wav" onChange={e=>accept(e.target.files[0])}/>{!file ? <div className={`dropzone ${drag?'dragging':''}`} onDragOver={e=>{e.preventDefault();setDrag(true)}} onDragLeave={()=>setDrag(false)} onDrop={e=>{e.preventDefault();setDrag(false);accept(e.dataTransfer.files[0])}}><span className="upload-icon"><UploadCloud/></span><h3>Upload a file to analyze</h3><p>Drag & drop an image, video, or audio file here</p><button className="btn btn-outline" onClick={()=>ref.current.click()}>Choose File</button><small>JPG, PNG, WEBP, MP4, MOV, MP3, WAV · Max 500 MB</small>{error&&<p className="form-error">{error}</p>}</div> : <div className="file-selected"><div className="file-pill">{iconFor(file.type)}<div><b>{file.name}</b><small>{file.type || 'media file'} · {(file.size/1024/1024).toFixed(2)} MB</small></div><button onClick={()=>setFile(null)} aria-label="Remove file"><Trash2 size={17}/></button></div>{file.type.startsWith('image') && <img className="file-preview" src={URL.createObjectURL(file)} alt="Selected media preview"/>}<div className="progress"><span/></div><div className="file-actions"><button className="text-button" onClick={()=>setFile(null)} disabled={loading}>Remove</button><button className="btn" onClick={submit} disabled={loading}>{loading ? 'Analyzing…' : 'Analyze media'} <LoaderCircle className={loading ? 'spin' : ''} size={16}/></button></div>{error&&<p className="form-error">{error}</p>}</div>}</div>;
}
